# Secrets — never commit
.env
.dev.vars

# Wrangler local state (may cache secrets/build output)
.wrangler/

# Dependencies
node_modules/

# OS / editor cruft
.DS_Store

# Vite build output
dist/

# TypeScript incremental build cache
*.tsbuildinfo

# Generated red-team corpora. `npm run corpus:thai` writes a few hundred
# harmful Thai prompts; the ThaiSafetyBench card says the data is "intended for
# academic purposes only", so the prompts stay out of git and out of a
# customer-facing repo. Regenerate from the script instead.
thai-corpus.csv
*.corpus.csv
